export type card = {
  picture_adress: string;
  name: string;
  explanation: string;
  related_url: string;
};

export type footer = {
  picture_adress: string;
  name: string;
  explanation: string;
  related_url: string;
};
